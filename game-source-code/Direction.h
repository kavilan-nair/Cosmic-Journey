#ifndef DIRECTION_H
#define DIRECTION_H
/**
* Direction is a strongly typed enum used by Player to move around the playing circle correctly
*/

enum class Direction
{
    CLOCKWISE,
    ANTICLOCKWISE,
    HOVER
};

#endif // Direction_H